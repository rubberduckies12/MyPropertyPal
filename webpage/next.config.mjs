/* @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: 'export', // Remove or comment out for normal build
  //assetPrefix: '/MyPropertyPal/', // Only needed for static export
  // basePath: '/MyPropertyPal',     // Only needed for static export
};

export default nextConfig;
